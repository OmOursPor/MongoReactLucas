name: CI

on:
  push:
    branches:
      - master
  pull_request:
    branches:
      - master

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: build docker-compose
        run: |
          cd nodejs/node-application
          docker-compose up -d --build
          


      
